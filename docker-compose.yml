version: "3.8"

services:
  alfo-claw:
    build: .
    container_name: alfo-claw-command-center
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
      - ./.env.local:/app/.env.local:ro
